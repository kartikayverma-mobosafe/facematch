# Headless OpenCV: this only ever decodes image bytes, never displays a window.
# The headless wheel drops the GUI/Qt bundle, which is a smaller install and one
# less reason to need libGL on a server.
opencv-python-headless
numpy
onnxruntime
insightface
fastapi
uvicorn[standard]
python-multipart
