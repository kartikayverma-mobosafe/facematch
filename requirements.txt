# Direct dependencies. Versions float here; requirements.lock.txt carries the
# exact set verified on the server, and is what a deployment should install.
#
# opencv is not listed: insightface depends on opencv-python itself, and naming
# it here as well only risks pulling in a second, conflicting copy.
numpy
onnxruntime
insightface
fastapi
uvicorn[standard]
python-multipart
